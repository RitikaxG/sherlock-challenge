services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sherlock
      POSTGRES_PASSWORD: sherlock
      POSTGRES_DB: sherlock_dev
    ports:
      - "5432:5432"
    volumes:
      - sherlock-postgres-data:/var/lib/postgresql/data

volumes:
  sherlock-postgres-data:
